# Examples

This folder contains generated public examples built only from synthetic fixture data.

- `synthetic-kb/`: a tiny generated KB that exercises source pages, topic pages, candidate pages, comparisons, backlinks, scrape-gap status, keyword search indexing, and deterministic intent-search indexing.
