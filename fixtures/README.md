# Fixtures

This folder contains synthetic public fixture inputs. They are not derived from the private WhatsApp exports, scraped artifacts, SQLite database, or generated private KB pages.

Use them only for smoke-testing the public repository.

The checked-in `examples/synthetic-kb/` pages are generated from this fixture.
