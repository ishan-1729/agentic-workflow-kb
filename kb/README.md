# Generated Knowledge Base

The generated Markdown KB is intentionally excluded from the public repository because it is derived from private local WhatsApp exports and scraped linked-content artifacts.

Run `scripts/build_kb.py` against a local `data/db/agentic_workflow.db` to regenerate it in this folder.
