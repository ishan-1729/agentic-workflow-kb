# Goal 000: PTY Smoke Test

This is a tiny goal used only to verify that `scripts/run_codex_goal.py` can host an interactive Codex CLI TUI, send `/goal`, and observe a done signal.

```text
/goal Verify interactive goal control by doing only this: write the file learnings/goal-000-pty-smoke.done containing the line GOAL_000_DONE, then stop. Do not edit any other file.
```

